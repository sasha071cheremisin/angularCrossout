import { FilterIncomePipe } from './filter-income.pipe';

describe('FilterIncomePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIncomePipe();
    expect(pipe).toBeTruthy();
  });
});
